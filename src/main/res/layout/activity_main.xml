<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/img_5"
    android:padding="24dp"
    tools:ignore="ExtraText">


    <!-- Start Quiz Button -->

    <!-- Fancy TextView with custom font -->

    <TextView
        android:id="@+id/fancyText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="120dp"
        android:fontFamily="ss"
        android:text="The World History App!"
        android:textColor="#F8F7F7"
        android:textSize="34sp"
        android:textStyle="bold"
        app:layout_constraintHorizontal_bias="0.459"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/startButton"
        android:layout_width="280dp"
        android:layout_height="63dp"
        android:layout_marginStart="40dp"
        android:layout_marginTop="544dp"
        android:background="@drawable/img"
        android:backgroundTint="@color/design_default_color_secondary"
        android:text="Lets Begin Quiz!"
        android:textSize="20sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="MissingConstraints" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="370dp"
        android:layout_height="62dp"
        android:layout_marginTop="64dp"
        android:layout_marginEnd="8dp"
        android:text="Using flashcards to make history an easy and enjoyable subject. Start and increase your knowledge!"
        android:textColor="@color/white"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/fancyText" />

    <!-- Correct constraints to center and position the button -->
    app:layout_constraintTop_toTopOf="parent"
    android:layout_marginTop="200dp"
    app:layout_constraintStart_toStartOf="parent"
    android:layout_marginStart="64dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
